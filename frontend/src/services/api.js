// Placeholder service for future API calls
export async function fetchProducts(params = {}) {
  // TODO: Replace with real API call
  return { items: [], total: 0 }
}

export async function fetchProductById(id) {
  // TODO: Replace with real API call
  return { id, name: `Product ${id}` }
}